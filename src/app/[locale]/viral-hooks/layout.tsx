import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Viral Hook Finder - Scroll-Stopping Hooks for Reels & TikTok",
  description: "Find the most viral hooks for your short-form videos. Generate AI hooks that grab attention in the first 3 seconds and skyrocket your video retention.",
  keywords: ["viral hooks", "instagram reel hooks", "tiktok hook generator", "scroll stopping hooks", "video opener ideas", "content marketing tools"],
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
